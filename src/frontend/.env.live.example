# Live (Production) Build Environment Configuration
# Copy this file to .env.live and use it for production builds
# Example: cp .env.live.example .env.live

# Publishing state: Set to 'live' for production deployments
# This enables search engine indexing and removes the draft indicator
VITE_PUBLISHING_STATE=live

# Visual effects configuration
# Set to 'false' (or omit) to enable full gradients and shadows for rich visuals
# Set to 'true' to disable advanced shading for simpler, flat visuals
VITE_DISABLE_ADVANCED_SHADING=false

# Card rendering mode configuration
# Options: 'dom' (default), 'background-image', or 'img'
# 'dom' provides the richest rendering with full DOM elements (recommended for production)
# 'background-image' uses CSS background-image for cards
# 'img' uses plain <img> elements for simpler draw and compute
VITE_CARD_RENDER_MODE=dom
