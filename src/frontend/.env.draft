# Draft build environment configuration
# This ensures draft deployments render the functional app UI with draft indicator
VITE_PUBLISHING_STATE=draft

# Visual effects configuration
# Set to 'true' to disable gradients and shadows for flat visuals in Solitaire
VITE_DISABLE_ADVANCED_SHADING=true

# Card rendering mode configuration
# Options: 'dom' (default), 'background-image', or 'img'
# 'img' uses plain <img> elements for simpler draw and compute
VITE_CARD_RENDER_MODE=img

# For live/production builds, see .env.live.example
